name : test

on: 
  - push

jobs:
  first_job:
    name: Print message
    runs-on: ubuntu-latest
    steps:
      - run: echo "Hello World!"
    
  second_job:
    name: Print more messages
    runs-on: ubuntu-latest
    steps:
      - run: echo "Hello" again
