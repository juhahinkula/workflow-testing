name: Hello Workflow

on: 
  workflow_run:
    workflows:
      - "Testing Workflows"
    types:
      - completed


jobs:
  third_job:
    runs-on: ubuntu-latest
    steps:
      - run: echo "This is another workflow"